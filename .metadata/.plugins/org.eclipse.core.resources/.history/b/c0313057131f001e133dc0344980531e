package classe;

public class Pessoa {

	// Desafio módulo: Criar 3 classes
	// classe pessoa -> nome da pessoas, peso, método comer vai receber parametro comida
	// acrescentar o peso da comida no método comer e acrescentar o peso na pessoa
	// classe comida -> 2 comidas, peso da comida
	// classe jantar -> método main, instanciar comida(feijão, peso)
		String nome;
		double pesoPessoa;
		double pesoC1;
		double pesoC2;
		
		double Comer(){
		return pesoC1 + pesoC2 + pesoPessoa;
		
	}
	
	public class Comida{
		String comida1;
		String comida2;
		
	}
}
